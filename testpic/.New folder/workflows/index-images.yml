name: Generate Image Manifest Index

on:
  push:
    paths:
      - 'images/**' # Run only when changes are made inside the images folder

jobs:
  build-manifest:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Allows the workflow to securely update your repo
    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Build JSON Manifest Array
        run: |
          cd images
          # Create a valid JSON array of image matches inside the directory
          echo '[' > temp.json
          first=true
          for file in *; do
            ext="${file##*.}"
            ext_lower=$(echo "$ext" | tr '[:upper:]' '[:lower:]')
            if [[ "$ext_lower" == "jpg" || "$ext_lower" == "jpeg" || "$ext_lower" == "png" || "$ext_lower" == "webp" || "$ext_lower" == "gif" ]]; then
              if [ "$first" = true ]; then
                first=false
              else
                echo ',' >> temp.json
              fi
              echo "\"$file\"" >> temp.json
            fi
          done
          echo ']' >> temp.json
          # Format output into a single clean line
          tr -d '\n' < temp.json | sed 's/, /,/g' > images.json
          rm temp.json

      - name: Commit and Push Image Index Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add images/images.json
          # Commit the index if updates were detected
          git diff-index --quiet HEAD || git commit -m "chore: automated asset index generation [skip ci]"
          git push
